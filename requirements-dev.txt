-r requirements.txt
ruff>=0.5
ipykernel>=6.29
jupyterlab>=4.2
# starlette.testclient (used by tests/test_api.py) needs one of httpx/httpx2
# present, and a from-scratch install can otherwise hit a hard RuntimeError
# instead of falling back gracefully. Pinning httpx2 explicitly here (a
# dev/test-only need, not a runtime dependency of the API itself) makes the
# test suite installable the same way everywhere.
httpx2>=0.1
