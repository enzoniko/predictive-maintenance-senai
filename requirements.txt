# Core scientific stack
numpy>=1.26
# NOTE: scipy is pinned below 1.18 for compatibility with the rest of the
# pinned stack (confirmed working: 1.15.x on Python 3.10, 1.17.x on Python
# 3.12). Re-validate before lifting this pin.
scipy<1.18
pandas>=2.2
# NOTE: left unpinned above a floor deliberately -- HistGradientBoosting's
# internal loss module has moved between scikit-learn releases, so a model
# bundle trained under one version cannot be unpickled under another (see
# models/bundle.py::ModelBundle.load and docs/03_arquitetura.md, section
# 3.6). A bundle is only ever portable across environments that resolve to
# the same scikit-learn version -- retrain locally with
# `python -m pdm.cli train` rather than copying a bundle from a different
# environment.
scikit-learn>=1.5
matplotlib>=3.8
pyyaml>=6.0
joblib>=1.4
typer>=0.12

# Signal processing
PyWavelets>=1.6

# Modeling (optional / platform-dependent, see src/pdm/*/optional_imports.py)
xgboost>=2.0
optuna>=3.6
mlflow>=2.14
shap>=0.45
lime>=0.2.0.1
# ssqueezepy pulls in numba/llvmlite, which need compiled extensions that
# not every environment has available. Code falls back to PyWavelets CWT
# when unavailable -- see src/pdm/features/wavelet.py.
ssqueezepy>=0.6.5

# Serving
fastapi>=0.111
uvicorn[standard]>=0.30
pydantic>=2.7
sqlalchemy>=2.0
streamlit>=1.36
requests>=2.32

# Data acquisition & document generation
gdown>=5.2
python-docx>=1.1
python-pptx>=0.6.23

# Dev / test
pytest>=8.2
nbclient>=0.10
nbformat>=5.10

# NOTE: the Postgres driver (psycopg2-binary) is intentionally NOT listed
# here. It needs pg_config/a C toolchain to build from source, which not
# every development environment has -- it would needlessly break
# `pip install -r requirements.txt` for a feature (docker-compose's
# Postgres backend) most local setups never exercise directly. It is added
# directly in docker/Dockerfile.api, which is where it actually runs.
# Local development uses the SQLite default in configs/default.yaml.
